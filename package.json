{
  "name": "nimiq-network",
  "version": "1.0.0",
  "description": "Nimiq Network",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd .. && git submodule foreach --recursive git checkout master && cd network && yarn gulp"
  },
  "keywords": [
    "nimiq",
    "network"
  ],
  "author": "Nimiq Foundation",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^4.0.0"
  },
  "dependencies": {
    "babel-core": "^6.23.0",
    "babel-preset-minify": "^0.3.0",
    "gulp-babel": "^7.0.1",
    "yargs": "^11.0.0"
  }
}
